{
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"nodemon --watch server --exec node server/server.js\"",
    "start": "node server/server.js",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
